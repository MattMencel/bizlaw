<%# New Hierarchical Navigation Partial %>
<nav class="bg-gray-800 text-white flex flex-col h-screen w-80 fixed left-0 top-0 z-50">
  <%= render 'shared/navigation/context_switcher' %>
  <%= render 'shared/navigation/menu_sections' %>
</nav>

<%# Mobile Navigation Toggle %>
<div class="lg:hidden fixed top-0 left-0 right-0 bg-gray-800 text-white p-4 z-40">
  <button type="button" 
          class="text-white hover:text-gray-300 focus:outline-none focus:ring-2 focus:ring-white"
          data-controller="mobile-navigation"
          data-action="click->mobile-navigation#toggle">
    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>
</div>